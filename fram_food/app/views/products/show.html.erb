<% provide :title, @product.name %>
 <div class="col-md-8">
   <ul class="products" id="current-product">
     <%= render @product %>
   </ul>z
   <% if logged_in? %>
     <% unless current_user.has_voted @product %>
       <% @votes.each do |key, value| %>
         <%= render "shared/rate_form", key: key %>
       <% end %>
     <% end %>
   <% end %>
 </div>
 <div class="clearfix"></div>
